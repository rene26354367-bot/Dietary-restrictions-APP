## 2026-04-30 OCR 解析器開發紀錄 (v3.1 - v3.2)
- **達成目標**: 完成具備視覺補正與多欄位偵測能力的 `NutritionParser.js`。
- **關鍵技術**:
    - **視覺補正**: 針對 OCR 常見誤判（O/0, I/1/l）在預處理階段進行補正。
    - **欄位鎖定**: 支援「每份」與「每 100g」雙欄位並存時的精準提取。
    - **高容錯 Regex**: 處理「脂盁」、「kca1」等掃描雜訊。
- **驗證結果**:
    - 成功解析 14 種不同規格的營養標示，包含 ASCII 表格、橫向流文字與中英對照。
    - Case 13 (嚴重誤判) 修正成功。
- **待優化問題**:
    - **Case 14 淨重干擾**: 在長文字流中，標籤前的「淨重」數字會干擾「糖/鈉」的抓取。
- **下一步計畫 (v3.3)**:
    - 實作「區段錨定 (Section Anchoring)」，強制解析器僅從「營養標示」關鍵字之後開始處理，排除前方干擾。

## 2026-05-01 區段錨定與前後端整合 (v3.3)
- **達成目標**: 解決長文字流中的雜訊干擾，並完成 UI 與後端引擎的完整對接。
- **關鍵更新**:
    - **區段錨定**: 在 `NutritionParser.js` 加入關鍵字切分邏輯，成功排除「淨重」、「成分」等前置數據干擾。
    - **API 修正**: 修復 `server.cjs` 中 OCR 解析結果的路徑錯誤（從 `result.data` 改為 `result.per1g`）。
    - **UI 聯動**: 確保 `AddFood.tsx` 能正確接收 OCR 基準（如每 100g/每份）並顯示於介面。
- **驗證結果**:
    - Case 14 (品名/成分夾雜) 完全正確，糖與鈉不再誤抓為 150。
    - 搜尋與資料儲存功能透過 API 運作正常。

## 2026-05-01 數據視覺化與趨勢分析優化 (v4.0)
- **達成目標**: 實現飲食攝取與體重變化的關聯分析，並強化達標率反饋。
- **關鍵更新**:
    - **體重趨勢整合**: 修改 `NutritionStats.tsx`，在 Recharts 圖表中加入雙 Y 軸設計。
    - **斷點連線處理**: 使用 `connectNulls` 屬性優化體重線。
    - **達標反饋系統**: 在歷史清單中實作彩色標籤。

## 2026-05-01 OCR 實體相機掃描與解析器重構 (v4.1)
... (略)

## 2026-05-02 OCR 系統架構級強化 (v5.3)
- **達成目標**: 實現工業級穩定性的影像前處理與數據校驗流程。
- **關鍵技術**:
    - **Stable High-Signal Pipeline**: 
        - 使用 `createImageBitmap` 實現後台解碼，降低主執行緒壓力。
        - 根據 `hardwareConcurrency` 動態決定 1800px/2400px 解析度上限，防止低階機 OOM。
        - 移除強制灰階，改用 `contrast(1.15)` 自適應強化，保護色彩特徵。
    - **Parser v5.3 (三層錨定)**:
        - 實作「精確、OCR 容錯、特徵」三層 ALIASES，解決「熟量」、「kca1」等掃描雜訊。
        - **數據自動交換**: 基於 `baseWeight` 的比例校驗，若比例反轉則自動交換數值。
    - **Sanity Check**: 實作熱量守恆 (±20% 容差) 與 `P+F+C <= 1.1` 硬性約束。
- **驗證結果**:
    - 通過 `test_ocr_parser_v6.js` 的異常案例測試。
    - 成功攔截並標記 photo_4, 5, 8 的典型錯誤。


